@file:Suppress("unused")

package com.vinish.nextup.ui.theme

import androidx.compose.ui.graphics.Color

// --- Core Brand / Primary (Classic Blue - preserved) ---
val PrimaryBlue = Color(0xFF1E6BFF)
val PrimaryBlueHover = Color(0xFF1557D6)
val PrimaryBlueLight = Color(0xFFEAF1FF)

// --- Core Brand / Primary (Green Nature Theme - new) ---
val PrimaryGreen = Color(0xFF2EB872)
val PrimaryGreenHover = Color(0xFF249D57)
val PrimaryGreenLight = Color(0xFFE8F5E9)
val PrimaryGreenDark = Color(0xFF166534)
val PrimaryGreenSubtle = Color(0xFFF0FDF4)
val BackgroundGreenLight = Color(0xFFF8FAF8)

// --- Neutrals & Backgrounds ---
val BackgroundLight = Color(0xFFF7F9FC)
val SurfaceWhite = Color(0xFFFFFFFF)
val SurfaceSubtle = Color(0xFFF1F5F9)

// --- Text Colors ---
val TextPrimary = Color(0xFF1E2430)
val TextSecondary = Color(0xFF717D96)
val TextTertiary = Color(0xFFA0AEC0)

// --- Borders & Dividers ---
val BorderLight = Color(0xFFE2E8F0)
val BorderMedium = Color(0xFFCBD5E1)

// --- Priority & Status Colors ---
// High Priority / Overdue (Red)
val PriorityHighText = Color(0xFFE53935)
val PriorityHighBg = Color(0xFFFFEBEE)

// Medium Priority (Orange)
val PriorityMediumText = Color(0xFFD97706)
val PriorityMediumBg = Color(0xFFFEF3C7)

// Low Priority / Personal (Green)
val PriorityLowText = Color(0xFF16A34A)
val PriorityLowBg = Color(0xFFDCFCE7)

// Due Today / Alert Banner
val AlertBannerBg = Color(0xFFFFF1F2)
val AlertBannerText = Color(0xFFE11D48)

// --- Category Colors (Icons & Accents) ---
val CategoryEducation = Color(0xFF1E6BFF)     // Deep Blue
val CategoryWork = Color(0xFF7C3AED)          // Indigo / Violet
val CategoryPersonal = Color(0xFFF97316)      // Orange / Coral
val CategoryFinance = Color(0xFF10B981)       // Mint Green
val CategoryHealth = Color(0xFFEF4444)        // Red
val CategorySubscriptions = Color(0xFFA855F7) // Purple
val CategoryDocuments = Color(0xFFF59E0B)     // Amber / Yellow
val CategoryTravel = Color(0xFF06B6D4)        // Cyan
val CategoryOther = Color(0xFF64748B)         // Slate Blue

// --- Progress & Checkboxes ---
val CheckboxChecked = PrimaryGreen
val CheckboxBorder = Color(0xFFCBD5E1)
val ProgressBarTrack = Color(0xFFE2E8F0)
val ProgressBarIndicator = PrimaryGreen

// border color
val BorderStoke = Color(0xFFF5F3F3)